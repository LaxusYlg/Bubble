import React, {Component} from "react";
import {Text, View} from "react-native";

export default class TabShotsScreen extends Component {
    render() {
        return (
            <View style={{flex: 1}}>
                <Text style={{height: 60, fontSize: 18}}>Tab</Text>
            </View>
        );
    }
}